apiVersion: v1
kind: Service
metadata:
  name: filemanager
  labels:
    app: filemanager
spec:
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: filemanager