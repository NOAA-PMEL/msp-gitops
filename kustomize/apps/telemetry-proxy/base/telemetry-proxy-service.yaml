apiVersion: v1
kind: Service
metadata:
  name: telemetry-proxy
  labels:
    app: telemetry-proxy
spec:
  selector:
    app: telemetry-proxy
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8081