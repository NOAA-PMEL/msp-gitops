# apiVersion: v1
# kind: PersistentVolume
# metadata:
#   name: filemanager-pv
#   # namespace: msp-system
#   labels:
#     type: local
# spec:
#   storageClassName: local-path
#   capacity:
#     storage: 1Gi
#   accessModes:
#     - ReadWriteOnce
#   hostPath:
#     path: "/data/envds/filemanager/base"
# ---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: erddap-content-storage
  # namespace: msp-system
spec:
  # storageClassName: local-path
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  volumeName: erddap-content-storage
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: erddap-data-pv-storage
  # namespace: msp-system
spec:
  # storageClassName: local-path
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  volumeName: erddap-data-pv-storage
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: erddap-datasetsd-storage
  # namespace: msp-system
spec:
  # storageClassName: local-path
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  volumeName: erddap-datasetsd-storage
