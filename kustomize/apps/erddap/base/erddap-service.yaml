apiVersion: v1
kind: Service
metadata:
  name: erddap
  # namespace: msp-system
  labels:
    app: erddap
spec:
  selector:
    app: erddap
  ports:
    - name: http
      port: 8080
      targetPort: 8080
    - name: https
      port: 8443
      targetPort: 8443
  type: ClusterIP  