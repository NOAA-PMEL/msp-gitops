apiVersion: v1
kind: ConfigMap
metadata:
  name: sampling-system
  # namespace: msp-system
data:
  daq_id: mspbase01
  # db_client_type: redis
  # db_client_hostname: redis.crk8s-system
  # db_client_port: '6379'
  # db_data_ttl: '300'
  # db_reg_device_definition_ttl: '0'
  # db_reg_device_instance_ttl: '300'
  
  # mqtt_broker: "0.0.0.0"
  # mqtt_port: "80"
  # mqtt_topic_filter: "instrument/#"
  # mqtt_topic_subscription: "instrument/#"
  # mqtt_broker: "mosquitto.default"
  # mqtt_port: "1883"
  # mqtt_topic_filter: "sensor/#"
  # mqtt_topic_subscription: "test-account/uas-test/#"
  # port: "8000"
  port: "8080"
  knative_broker: "http://broker-ingress.knative-eventing.svc.cluster.local/mspbase01-system/default"

  mqtt_broker: "mqtt.mspbase01-system"
  mqtt_topic_subscriptions: >
    envds/+/+/+/data/update,   
    envds/+/+/+/controller/data/update   
  
  mqtt_port: "1883"
