apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
metadata:
  name: sampling-system

resources:
- sampling-system-deployment.yaml
- sampling-system-service.yaml
- ../../shared-libs/calculations/base
# Remove the knative reference:
# - sampling-system-service_knative.yaml

configMapGenerator:
  - name: sampling-system-calculations
    files:
      - ../../shared-libs/calculations/base/default.py