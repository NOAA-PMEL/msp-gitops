apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
metadata:
  name: sampling-system

resources:
- sampling-system-deployment.yaml
- sampling-system-service.yaml
# Remove the knative reference:
# - sampling-system-service_knative.yaml

configMapGenerator:
  - name: shared-calculations-config
    namespace: envds
    files:
      # Point UP and OVER to the shared folder
      - ../../shared-libs/calculations/default.py