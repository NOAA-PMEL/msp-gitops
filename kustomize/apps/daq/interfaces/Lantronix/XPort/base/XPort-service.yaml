apiVersion: v1
kind: Service
metadata:
  name: lantronix-xport
  # namespace: msp
  labels:
    app: lantronix-xport
spec:
  selector:
    app: lantronix-xport
  ports:
  - name: http
    protocol: TCP
    port: 9080
    targetPort: 9080
  # - name: https
  #   port: 8883
  #   targetPort: 8883
  # type: LoadBalancer
  type: ClusterIP