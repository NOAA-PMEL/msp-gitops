apiVersion: v1
kind: ConfigMap
metadata:
  name: lantronix-xport-config
  # namespace: msp
  labels:
    app: lantronix-xport
data:
  namespace_prefix: revpi1
  envds_env_id: revpi1
  interface.conf: |
    uid: spidermagic810-017
    # serial_number: "1234"
    # daq_id: clearsky
    host: 10.55.179.232
    paths:
      port-1:
          # host: 10.55.169.52
          # host: 192.168.86.46 # can override interface host if needed?
          port: 10001 # if omitted, default is used
