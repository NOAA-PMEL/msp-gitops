apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
metadata:
  name: synaccess-NP05B

# Example configuration for the webserver
# at https://github.com/monopole/hello
# commonLabels:
#   app: nginx

resources:
#- USCDR301-configmap.yaml
- NP05B-deployment.yaml
- NB05B-service.yaml

configMapGenerator:
  - name: controller-definition
    files:
      - Synaccess_NP05B_controller_definition.json