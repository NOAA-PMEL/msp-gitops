apiVersion: v1
kind: Service
metadata:
  name: shelly-shellymock
  # namespace: msp
  labels:
    app: shelly-shellymock
spec:
  selector:
    app: shelly-shellymock
  ports:
  - name: http
    protocol: TCP
    port: 9080
    targetPort: 9080
  # - name: https
  #   port: 8883
  #   targetPort: 8883
  # type: LoadBalancer
  type: ClusterIP