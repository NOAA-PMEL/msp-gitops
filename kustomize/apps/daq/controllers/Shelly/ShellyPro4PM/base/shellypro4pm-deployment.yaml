apiVersion: apps/v1
kind: Deployment
metadata:
  name: shelly-shellypro4pm
  # namespace: msp
  labels:
    app: shelly-shellypro4pm
spec:
  selector:
    matchLabels:
      app: shelly-shellypro4pm
  template:
    metadata:
      labels:
        app: shelly-shellypro4pm
    spec:
      imagePullSecrets:
      - name: ghcr-ro-secret
      containers:
      - name: shelly-shellypro4pm
        # image: registry-msp:5000/msp/interface/shelly-shellypro3:0.1.0
        image: ghcr.io/noaa-pmel/msp/shelly-shellypro4pm:0.1.0
        imagePullPolicy: "Always"
        env:
          - name: ENVDS_CORE_NAMESPACE_PREFIX
            valueFrom:
              configMapKeyRef:
                name: shelly-shellypro4pm-config
                key: namespace_prefix
          - name: ENVDS_ENV_ID
            valueFrom:
              configMapKeyRef:
                name: shelly-shellypro4pm-config
                key: envds_env_id
            # value: mspbase01
          - name: REDIS_OM_URL
            value: redis://redis.default
          - name: LOG_LEVEL
            value: DEBUG
        resources:
          requests:
            cpu: "50m"
          limits:
            memory: "128Mi"
            cpu: "500m"
        ports:
        - name: http
          containerPort: 9080
          protocol: TCP
        # - name: https
        #   containerPort: 8883
        #   protocol: TCP
        volumeMounts:
            - name: shelly-shellypro4pm-config
              mountPath: /app/config/controller.conf
              subPath: controller.conf
            - name: controller-definition
              mountPath: /app/Shelly_ShellyPro4PM_controller_definition_1_0_0.json
              subPath: Shelly_ShellyPro4PM_controller_definition_1_0_0.json
            # - name: mosquitto-certs
            #   mountPath: /certs/ca.crt
            #   subPath: ca.crt
            # - name: mosquitto-certs
            #   mountPath: /certs/server.crt
            #   subPath: server.crt
            # - name: mosquitto-certs
            #   mountPath: /certs/server.key
            #   subPath: server.key
      volumes:
        - name: shelly-shellypro4pm-config
          configMap:
            name: shelly-shellypro4pm-config
        - name: controller-definition
          configMap:
            name: controller-definition
        # - name: mosquitto-certs
        #   configMap:
        #     name: mosquitto-certs
