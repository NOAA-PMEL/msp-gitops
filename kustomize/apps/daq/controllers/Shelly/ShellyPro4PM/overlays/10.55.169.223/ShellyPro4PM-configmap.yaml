apiVersion: v1
kind: ConfigMap
metadata:
  name: shelly-shellypro4pm-config
  # namespace: msp
  labels:
    app: shelly-shellypro4pm
data:
  namespace_prefix: revpi2
  envds_env_id: revpi2
  controller.conf: |
    uid: shel223
    host: 10.55.169.223
    client_host: mqtt.revpi2-system
    client_port: 1883
    controller_id_prefix: shellypro4pm

    # override definition values
    attributes:
      serial_number: shel223
    # variables:
    #   channel_2_power:
    #     default_value: "0"
