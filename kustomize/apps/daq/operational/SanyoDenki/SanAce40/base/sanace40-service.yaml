apiVersion: v1
kind: Service
metadata:
  name: sanyodenki-sanace40
  # namespace: msp
  labels:
    app: sanyodenki-sanace40
spec:
  selector:
    app: sanyodenki-sanace40
  ports:
  - name: http
    protocol: TCP
    port: 9080
    targetPort: 9080
  # - name: https
  #   port: 8883
  #   targetPort: 8883
  # type: LoadBalancer
  type: ClusterIP