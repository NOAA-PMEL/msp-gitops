apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
metadata:
  name: dwyer-scd1000

resources:
- scd1000-deployment.yaml
- scd1000-service.yaml

configMapGenerator:
  - name: operational-definition
    files:
      - Dwyer_SCD_operational_definition.json
