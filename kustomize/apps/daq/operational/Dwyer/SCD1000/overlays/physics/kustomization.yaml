apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
metadata:
  name: dwyer-scd1000

namespace: payload01-hardware

resources:
- ../../base

nameSuffix: "-payload01"

# Generate the ConfigMap dynamically so it gets a hash suffix
configMapGenerator:
  - name: dwyer-scd1000-config
    literals:
      - namespace_prefix=payload01
      - envds_env_id=payload01
    files:
      - operational.conf


# apiVersion: kustomize.config.k8s.io/v1beta1
# kind: Kustomization
# metadata:
#   name: dwyer-scd1000

# namespace: payload01-hardware

# resources:
# - ../../base
# - scd1000-configmap.yaml

# nameSuffix: "-payload01"

# patches:
#   # change configmap names to account for s/n suffix
#   - target:
#       kind: Deployment
#       name: dwyer-scd1000
#     patch: |-
#       - op: replace 
#         path: /spec/template/spec/volumes/0/configMap/name
#         value: dwyer-scd1000-config-payload01
#       - op: replace 
#         path: /spec/template/spec/volumes/1/configMap/name
#         value: operational-definition-payload01