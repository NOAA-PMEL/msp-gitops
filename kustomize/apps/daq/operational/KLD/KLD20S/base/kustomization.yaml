apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
metadata:
  name: kld-kld20s

resources:
- kld20s-deployment.yaml
- kld20s-service.yaml

configMapGenerator:
  - name: operational-definition
    files:
      - KLD_KLD20S_operational_definition.json