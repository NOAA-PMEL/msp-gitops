apiVersion: v1
kind: Service
metadata:
  name: ist-fs7
  # namespace: msp
  labels:
    app: ist-fs7
spec:
  selector:
    app: ist-fs7
  ports:
  - name: http
    protocol: TCP
    port: 9080
    targetPort: 9080
  # - name: https
  #   port: 8883
  #   targetPort: 8883
  # type: LoadBalancer
  type: ClusterIP