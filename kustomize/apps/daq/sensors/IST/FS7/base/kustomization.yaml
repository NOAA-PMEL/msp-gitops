apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
metadata:
  name: ist-fs7

# Example configuration for the webserver
# at https://github.com/monopole/hello
# commonLabels:
#   app: nginx

resources:
- fs7-deployment.yaml
- fs7-service.yaml

configMapGenerator:
  - name: sensor-definition
    files:
      - IST_FS7_sensor_definition.json