apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
metadata:
  name: ist-hyt939p


namespace: mspbase02-hardware
# namespace: crk8s-hardware

# Example configuration for the webserver
# at https://github.com/monopole/hello
# commonLabels:
#   app: nginx

# resources:
# # Example configuration for the webserver
# # at https://github.com/monopole/hello
#   nameSuffix: -usc206
#   commonLabels:
#     app: usconverters-uscdr301
#     variant: test

resources:
- ../../base
- hyt939p-configmap.yaml

nameSuffix: "-1234"

# patchesStrategicMerge:
#   - deployment.yaml

patches:
  # change configmap names to account for s/n suffix
  - target:
      kind: Deployment
      name: ist-hyt939p
    patch: |-
      - op: replace 
        path: /spec/template/spec/volumes/0/configMap/name
        value: ist-hyt939p-config-1234
      - op: replace 
        path: /spec/template/spec/volumes/1/configMap/name
        value: sensor-definition-1234
