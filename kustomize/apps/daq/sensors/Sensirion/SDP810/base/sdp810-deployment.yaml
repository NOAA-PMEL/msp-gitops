apiVersion: apps/v1
kind: Deployment
metadata:
  name: sensirion-sdp810
  # namespace: msp
  labels:
    app: sensirion-sdp810
spec:
  selector:
    matchLabels:
      app: sensirion-sdp810
  template:
    metadata:
      labels:
        app: sensirion-sdp810
    spec:
      imagePullSecrets:
      - name: ghcr-ro-secret
      containers:
      - name: sensirion-sdp810
        image: ghcr.io/noaa-pmel/msp/sensirion-sdp810:0.1.0
        imagePullPolicy: "Always"
        env:
          - name: ENVDS_CORE_NAMESPACE_PREFEIX
            valueFrom:
              configMapKeyRef:
                name: sensirion-sdp810-config
                key: namespace_prefix
          - name: ENVDS_ENV_ID
            valueFrom:
              configMapKeyRef:
                name: sensirion-sdp810-config
                key: envds_env_id
          - name: REDIS_OM_URL
            value: redis://redis.default
          - name: LOG_LEVEL
            value: DEBUG
        resources:
          requests:
            cpu: "50m"
          limits:
            memory: "128Mi"
            cpu: "500m"
        ports:
        - name: http
          containerPort: 9080
          protocol: TCP
        # - name: https
        #   containerPort: 8883
        #   protocol: TCP
        volumeMounts:
            - name: sensirion-sdp810-config
              mountPath: /app/config/sensor.conf
              subPath: sensor.conf
            # - name: mosquitto-certs
            #   mountPath: /certs/ca.crt
            #   subPath: ca.crt
            # - name: mosquitto-certs
            #   mountPath: /certs/server.crt
            #   subPath: server.crt
            # - name: mosquitto-certs
            #   mountPath: /certs/server.key
            #   subPath: server.key
      volumes:
        - name: sensirion-sdp810-config
          configMap:
            name: sensirion-sdp810-config
        # - name: mosquitto-certs
        #   configMap:
        #     name: mosquitto-certs