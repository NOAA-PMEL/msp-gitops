apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
metadata:
  name: aerosoldynamics-spidermagic810

# Example configuration for the webserver
# at https://github.com/monopole/hello
# commonLabels:
#   app: nginx

resources:
- spidermagic810-deployment.yaml
- spidermagic810-service.yaml

configMapGenerator:
  - name: sensor-definition
    files:
      - AerosolDynamics_SPIDERMAGIC810_sensor_definition.json