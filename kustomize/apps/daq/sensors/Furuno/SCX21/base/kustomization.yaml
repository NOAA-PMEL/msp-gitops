apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
metadata:
  name: furuno-scx21

# Example configuration for the webserver
# at https://github.com/monopole/hello
# commonLabels:
#   app: nginx

resources:
- scx21-deployment.yaml
- scx21-service.yaml

configMapGenerator:
  - name: sensor-definition
    files:
      - Furuno_SCX21_sensor_definition.json