apiVersion: v1
kind: Service
metadata:
  name: phoenixcontact-ntp
  # namespace: msp
  labels:
    app: phoenixcontact-ntp
spec:
  selector:
    app: phoenixcontact-ntp
  ports:
  - name: http
    protocol: TCP
    port: 9080
    targetPort: 9080
  # - name: https
  #   port: 8883
  #   targetPort: 8883
  # type: LoadBalancer
  type: ClusterIP