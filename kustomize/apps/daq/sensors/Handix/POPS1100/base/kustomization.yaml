apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
metadata:
  name: handix-pops1100

# Example configuration for the webserver
# at https://github.com/monopole/hello
# commonLabels:
#   app: nginx

resources:
- pops1100-deployment.yaml
- pops1100-service.yaml

configMapGenerator:
  - name: sensor-definition
    files:
      - Handix_POPS1100_sensor_definition.json