apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
metadata:
  name: vaisala-aqt560

# Example configuration for the webserver
# at https://github.com/monopole/hello
# commonLabels:
#   app: nginx

resources:
- aqt560-deployment.yaml
- aqt560-service.yaml

configMapGenerator:
  - name: sensor-definition
    files:
      - Vaisala_AQT560_sensor_definition.json
