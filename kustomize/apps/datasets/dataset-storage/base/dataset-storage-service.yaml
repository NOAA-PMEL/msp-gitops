apiVersion: v1
kind: Service
metadata:
  name: dataset-storage
  labels:
    app: dataset-storage
spec:
  selector:
    app: dataset-storage
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
      name: http