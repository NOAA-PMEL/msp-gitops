apiVersion: v1
kind: Service
metadata:
  name: sampling-states
  labels:
    app: sampling-states
spec:
  selector:
    app: sampling-states
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080