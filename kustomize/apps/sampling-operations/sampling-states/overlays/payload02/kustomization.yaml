apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: payload02-system

resources:
- ../../base
- sampling-states-configmap.yaml

configMapGenerator:
  - name: sampling-states-states
    files:
      # Use the fully decoupled state definitions
      - ../../shared_configs/templates/states/24v_bus_power_stabilized_state.json
      - ../../shared_configs/templates/states/tec_power_stabilized_state.json
      - ../../shared_configs/templates/states/neph_power_stabilized_state.json
      - ../../shared_configs/templates/states/absorb_power_off_stabilized_state.json
      - ../../shared_configs/templates/states/neph_power_off_stabilized_state.json
      - ../../shared_configs/templates/states/tec_power_off_stabilized_state.json
      - ../../shared_configs/templates/states/maintenance_active_state.json

generatorOptions:
  disableNameSuffixHash: true

nameSuffix: "-payload02"

patches:
  - target:
      kind: Service
      name: sampling-states
    patch: |-
      - op: replace 
        path: /spec/template/spec/volumes/0/configMap/name
        value: sampling-states-states-payload02
  - target:
      kind: Trigger
    patch: |-
      - op: replace 
        path: /spec/subscriber/ref/name
        value: sampling-states-payload02

# apiVersion: kustomize.config.k8s.io/v1beta1
# kind: Kustomization
# metadata:
#   name: sampling-states

# # set namespace
# namespace: payload02-system

# # Example configuration for the webserver
# # at https://github.com/monopole/hello
# # commonLabels:
# #   app: nginx

# # resources:
# # # Example configuration for the webserver
# # # at https://github.com/monopole/hello
# #   nameSuffix: -usc206
# #   commonLabels:
# #     app: usconverters-uscdr301
# #     variant: test

# resources:
# - ../../base
# - sampling-states-configmap.yaml
# # - platform_defs_configmap.yaml
# # - platform_varmaps_configmap.yaml
# # - ghcr-secret-msp-system-sealed.yaml
# # uncomment this if using knative for data updates
# # - sampling-states-triggers.yaml

# configMapGenerator:
#   - name: sampling-states-states
#     files:
#       - sampling_states.json

# nameSuffix: "-payload02"

# patches:
#   # change configmap names to account for s/n suffix
#   - target:
#       # version: serving.knative.dev/v1
#       kind: Service
#       name: sampling-states
#     patch: |-
#       # - op: replace 
#       #   path: /spec/template/spec/volumes/0/configMap/name
#       #   value: sampling-states-payload02
#       - op: replace 
#         path: /spec/template/spec/volumes/0/configMap/name
#         value: sampling-states-states-payload02

# configurations:
#   - config.yaml
