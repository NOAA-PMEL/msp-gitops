apiVersion: v1
kind: Service
metadata:
  name: sampling-modes
  labels:
    app: sampling-modes
spec:
  selector:
    app: sampling-modes
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080