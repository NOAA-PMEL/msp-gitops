apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
metadata:
  name: sampling-operations

# Example configuration for the webserver
# at https://github.com/monopole/hello
# commonLabels:
#   app: nginx

resources:
# - sampling-operations-deployment.yaml
# - sampling-operations-service.yaml
- sampling-operations-service_knative.yaml
# - sampling-operations-ingress.yaml
# - sampling-operations-triggers.yaml