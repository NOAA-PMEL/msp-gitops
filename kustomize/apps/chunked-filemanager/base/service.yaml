apiVersion: v1
kind: Service
metadata:
  name: chunked-filemanager
  labels:
    app: chunked-filemanager
spec:
  selector:
    app: chunked-filemanager
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
      name: http